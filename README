Multiplex README:

Source code repo: https://github.com/blizzardplus/multiplex

1.1 How to get the code:
1.1.2 Ubuntu:

git clone https://github.com/blizzardplus/multiplex

Current development is done on "dev_multiple_node" so make sure you checkout that branch

git checkout dev_multiple_node 

1.2 How to compile:

1.2.1 Dependencies:

Lib Boost:

Multiplex uses Boost C++ library:

On Ubuntu use the following command to install Boot library:

sudo apt-get install libboost-all-dev

The source code should be linked with -lboost_system. A sample Makefile is included in the "src" folder which will be replace with a cofig file.

1.3 How to run and test

The executable is called Proxifier. Run Proxifier with any of values X = { 1, 2, 3, 4}:

./Proxifier X 

Argument = 1: indicates this is the client application
Argument = 2: indicates this is the first middle hop
Argument = 3: indicates this is the second middle hop
Argument = 3: indicates this is the end hop

To test the setup call this:

nc -X 5 -x 127.0.0.1:5555 google.com 80
